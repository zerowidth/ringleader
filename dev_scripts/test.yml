---
test:
  dir: "./dev_scripts"
  # command: "ncat -k -l 10001"
  command: "bundle exec foreman start"
  # command: sleep 10
  server_port: 10000
  app_port: 10001
  idle_timeout: 5
  startup_timeout: 5
  kill_with: "INT"
